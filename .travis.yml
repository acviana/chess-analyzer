language: python
python:
  - "3.8"
install:
  - pip install -U pip
  - pip install -r requirements.txt
  - pip install -r requirements_dev.txt
  - pip install codecov
script:
  - pytest test/
after_success:
  - codecov
